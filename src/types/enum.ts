export enum VerificationStatus {
  Pending = "pending",
  Verified = "verified",
  Rejected = "rejected",
}

export enum TripStatus{
    ACTIVE="ACTIVE",
    STARTED="STARTED",
    COMPLETED="COMPLETED",
    CANCELLED="CANCELLED"
}

export enum OrderStatus {
  ACCEPTED = "ACCEPTED",
  COMPLETED = "COMPLETED",
  CANCELLED = "CANCELLED"
}